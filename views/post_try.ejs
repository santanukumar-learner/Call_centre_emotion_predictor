<!-- <!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Upload Audio File</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: #f5f5f5;
    }
    .container {
      max-width: 500px;
      margin-top: 100px;
      background: white;
      padding: 30px;
      border-radius: 15px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
    }
  </style>
</head>
<body>
  <div class="container text-center">
    <h3>Upload Your Audio</h3>
    <form action="/uploadsample" method="POST" enctype="multipart/form-data">
      <div class="mb-3 text-start">
        <label for="audio" class="form-label">Choose an audio file:</label>
        <input class="form-control" type="file" name="audio" id="audio" accept="audio/*" required>
      </div>
      <button type="submit" class="btn btn-primary mt-2">Upload</button>
    </form>
  </div>
</body>
</html> -->




<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Upload Audio File</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: #f5f5f5;
    }
    .container {
      max-width: 500px;
      margin-top: 100px;
      background: white;
      padding: 30px;
      border-radius: 15px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
    }
    #loadingSpinner {
      display: none;
    }
  </style>
</head>
<body>
  <div class="container text-center">
    <h3>Upload Your Audio</h3>
    <form id="uploadForm" action="/uploadsample" method="POST" enctype="multipart/form-data">
      <div class="mb-3 text-start">
        <label for="audio" class="form-label">Choose an audio file:</label>
        <input class="form-control" type="file" name="audio" id="audio" accept="audio/*" required>
      </div>
      <button type="submit" id="submitBtn" class="btn btn-primary mt-2">Upload</button>

      <!-- Loading Spinner -->
      <div id="loadingSpinner" class="mt-3">
        <div class="spinner-border text-primary" role="status">
          <span class="visually-hidden">Uploading...</span>
        </div>
        <p class="mt-2">Processing your audio...</p>
      </div>
    </form>
  </div>

  <script>
    const form = document.getElementById('uploadForm');
    const spinner = document.getElementById('loadingSpinner');
    const submitBtn = document.getElementById('submitBtn');

    form.addEventListener('submit', () => {
      submitBtn.disabled = true;
      spinner.style.display = 'block';
    });
  </script>
</body>
</html>
